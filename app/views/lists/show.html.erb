<div class="list__container">
    <div class="list__content__container">
        <h3><%= @list.name %></h3>
        <% @list.tasks.all.each do |task| %>
            <h4>
            <%= link_to (task.complete == true ? "✔️" : "🔲"), "/tasks/#{task.id}", method: :get %>
            <%= task.name  %>
            <%= link_to "delete", "/tasks/#{task.id}", method: :delete %></h4>
        <% end %>
        <div class="list__form__container">
            <%= form_for :task do |f| %>
                <%= text_field_tag :task_name, nil, placeholder: "new task" %><br />
                <%= f.submit "Create list", class: "new__task__button" %>
            <% end %>
        </div>
    </div>
</div>
 